
# Core Django
Django
python-decouple

# Document Processing
PyMuPDF
python-docx
openpyxl

# AI/ML - GPU-aware versions
# For GPU (CUDA 12.x - RTX 5050 and newer):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install faiss-gpu-cu12
# For CPU-only fallback:
#   pip install torch faiss-cpu
sentence-transformers
torch
faiss-cpu

# Ollama client
ollama

# Task Scheduling
django-apscheduler

# Utilities
python-magic
Pillow
requests

# Development
django-debug-toolbar

# Code IDE - syntax highlighting and code processing
pygments
