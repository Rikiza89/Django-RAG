# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite by default)
DATABASE_URL=sqlite:///db.sqlite3

# ── Ollama — Knowledge RAG ────────────────────────────────────────────────
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b
# GPU with more VRAM: llama3.1:8b-instruct-q4_K_M
OLLAMA_TIMEOUT=180

# ── Ollama — Coding IDE ───────────────────────────────────────────────────
CODING_OLLAMA_HOST=http://localhost:11434
CODING_OLLAMA_MODEL=qwen2.5-coder:14b-instruct-q4_K_M
# Lighter alternative: qwen2.5-coder:7b-instruct-q4_K_M
CODING_OLLAMA_TIMEOUT=480

# ── Embedding Model ───────────────────────────────────────────────────────
# Multilingual (100+ languages, 1024 dims, ~570 MB) — recommended
EMBEDDING_MODEL=BAAI/bge-m3
# English-only alternative (1024 dims, ~1.3 GB):
# EMBEDDING_MODEL=BAAI/bge-large-en-v1.5
# Lightweight alternative (768 dims, ~420 MB):
# EMBEDDING_MODEL=sentence-transformers/all-mpnet-base-v2
MODELS_CACHE_DIR=./models_cache
# Embeddings run on CPU — GPU VRAM is reserved for Ollama LLM inference
EMBEDDING_DEVICE=cpu
EMBEDDING_BATCH_SIZE=64

# ── FAISS — Knowledge RAG ─────────────────────────────────────────────────
FAISS_INDEX_PATH=./faiss_index
FAISS_TOP_K=5
CHUNK_SIZE=800
CHUNK_OVERLAP=150

# ── FAISS — Coding IDE ────────────────────────────────────────────────────
CODE_FAISS_INDEX_PATH=./code_faiss_index
CODE_FAISS_TOP_K=8
CODE_CHUNK_SIZE=1000
CODE_CHUNK_OVERLAP=200
CODE_ENABLE_QUERY_CACHE=True
CODE_QUERY_CACHE_SIZE=100

# ── Memory / Cache ────────────────────────────────────────────────────────
# 32 GB RAM defaults — reduce if on a lower-memory machine
MAX_CONCURRENT_EMBEDDINGS=1
ENABLE_QUERY_CACHE=True
QUERY_CACHE_SIZE=200

# ── Upload Settings ───────────────────────────────────────────────────────
# Value is in MB (settings.py multiplies by 1024*1024)
MAX_UPLOAD_SIZE=100
ALLOWED_EXTENSIONS=.pdf,.docx,.txt,.xlsx
